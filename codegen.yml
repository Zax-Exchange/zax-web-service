overwrite: true
schema: ./app/graphql/schema/**/*.ts
generates:
  ./app/graphql/resolvers-types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers
